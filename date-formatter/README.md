# date-formatter

Date and time utilities for Pipe.

## Functions

- `format_now layout` — Format current time (e.g., "2006-01-02 15:04:05")
- `relative_time ts` — Human-readable relative time ("vor 3 Stunden")
- `is_weekend ts` — Check if a timestamp falls on weekend
